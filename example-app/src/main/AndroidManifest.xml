<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.gov.dfat.vdsncchecker.example">
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VDSNCCheckerExample">
        <activity
            android:name="au.gov.dfat.vdsncchecker.example.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="au.gov.dfat.vdsncchecker.example.ScanActivity" />
        <activity android:name="au.gov.dfat.lib.vdsncchecker.ui.ScanSettingsActivity" />
        <activity android:name="au.gov.dfat.vdsncchecker.example.DetailActivity" />
        <activity android:name="au.gov.dfat.vdsncchecker.example.AttributionsActivity" />
    </application>

</manifest>